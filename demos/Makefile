all:
	(cd timerLib && make install)
	(cd 1-led && make)
	(cd 2* && make)
	(cd 3-led-blink && make)
	(cd 3-led-blink-slower && make)
	(cd 4* && make)
	(cd 5-led-fn-modular && make)
	(cd 5-led-fn-modular-practice && make)
	(cd 6* && make)
	(cd 7* && make)
	(cd 8-switch && make)
	(cd 8-switch-playground && make)
	(cd 9* && make)
	(cd 10* && make)
	(cd 11-buzzer && make)
	(cd 11-buzzer-playground && make)

clean:
	(cd timerLib && make clean)
	(cd 1-led && make clean)
	(cd 2* && make clean)
	(cd 3-led-blink && make clean)
	(cd 3-led-blink-slower && make clean)
	(cd 4* && make clean)
	(cd 5-led-fn-modular && make clean)
	(cd 5-led-fn-modular-practice && make clean)
	(cd 6* && make clean)
	(cd 7* && make clean)
	(cd 8-switch && make clean)
	(cd 8-switch-playground && make clean)
	(cd 9* && make clean)
	(cd 10* && make clean)
	(cd 11-buzzer && make clean)
	(cd 11-buzzer-playground && make clean)
	rm -rf h lib
